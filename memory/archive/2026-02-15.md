
## Azure cost cut
- Verified VM: Standard_B2als_v2 (UAE North). OS disk was Premium_LRS 64GB.
- Changed OS disk SKU to StandardSSD_LRS to cut costs. Confirmed disk now StandardSSD_LRS tier Standard, 500 IOPS / 100 MB/s.

## LifeOS 0.1.7 shipped + follow-up fixes
- Shipped mvp-0.1.7 release (tool trace + per-message Danger exec + collapsible Thinking/Trace). Then fixed SSE buffering + removed structured JSON output to avoid raw JSON blobs; filtered OpenClaw internal errors with retries; improved trace formatting; fixed Dart string escape; rebuilt.
- Added HEAD support to /music/stream/{id} endpoint (GET/HEAD) so curl -I works.

## LifeOS mvp-0.1.8 (GitHub Builds + Update Checker)
- Moved heavy Android builds to GitHub Actions to prevent VPS CPU overload/freezing.
- Updated `.github/workflows/android-apk.yml` to build optimized Release APKs on `mvp-*` tags.
- Integrated `package_info_plus` and `url_launcher` into Flutter app.
- Added "Check for Updates" section in Settings: hits GitHub API to notify of newer releases and provide direct download links.
- Shipped mvp-0.1.8 release via GitHub Actions.

## Infrastructure / Dev Safety
- **CRITICAL**: Do NOT run `flutter build` or heavy compilation tasks directly on the VPS. It will freeze and requires a manual restart. Always use GitHub Actions for builds.

## OpenClaw auth/model routing
- Removed noisy Antigravity OAuth profile (makoridylan@gmail.com). Reordered fallbacks so Codex/Groq precede OpenRouter free to avoid free-tier caps.

## Telegram-backed Music module (Resonance Stream)
- Created Telegram supergroup **Resonance Stream**; actual chat_id confirmed: **-1003752810090**. Bot made admin.
- Built backend music module:
  - SQLite DB with FTS5 search, artists/albums, favorites; stream endpoint proxies Telegram file.
  - Bot polling endpoint /music/poll indexes new audio via getUpdates.
  - Telethon user auth required for full history scan; bots cannot call GetHistoryRequest.
- Implemented streaming with HTTP Range support (206 Partial Content + Content-Range). Confirmed Range works for a forwarded FLAC.
- Implemented bot-driven cleanup: deleted indexed audio messages from group via deleteMessage (14 messages) to allow re-forwarding.
- Nuked music.db and rebuilt clean so all tracks have bot_file_id and are fully streamable; after re-forwarding, re-polled: total 18 tracks, missing_bot_file_id=0.
- Added ffmpeg/ffprobe to VPS and new endpoint **/music/assess/{track_id}** returning a “quality card” (codec/sample rate/bit depth/channels/bitrate). Verified track 1 is lossless FLAC 44.1kHz/16-bit.
- Added **/music/import/url** endpoint: download direct URL → ffprobe assess → upload to Telegram group via bot; then /music/poll indexes it for streaming.

## LifeOS Music UI (in progress)
- Implemented Flutter changes (committed, CI building): new Music tab + global mini-player dock (pushes content up), using just_audio. Dock persists across Chat/Music/Notes/Finance/Terminal.

## Music Module Roadmap (Roon-like 0$ Vision)
- **Lyrics**: Plan to integrate LRCLIB (free/open, synced lyrics).
- **Metadata**: Plan to use MusicBrainz for deep credits, bios, and artist relationships.
- **Visuals**: Dynamic color extraction and high-res art (Fanart.tv).

## Policy/UX note
- Dylan proposed automating ingestion by having our bot fetch music from another bot. Flagged that "we don't know how it sources" isn't a safe/legal loophole; recommended source-agnostic import plumbing + allowlist/manual confirmation guardrails.

## Product/UX notes
- Dylan wants Music as its own tab with a persistent mini-player to allow listening while using Chat/Notes.
- Dylan interested in import-by-URL pipeline (source-agnostic), quality assessment, and later lyrics.
- Discussed that deezload/DRM bypass is out-of-bounds; build neutral import plumbing instead.
