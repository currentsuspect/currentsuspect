# 2026-03-09

- Fixed bash startup issues on the VPS caused by alias/function collisions: `zi` alias conflicted with zoxide’s generated `zi()` function, `lt` alias conflicted with PM2 log function, and `mem` alias conflicted with `mem()` helper. Renamed/adjusted helpers to `ltree`, `plog`, `mem-usage`, and kept zoxide-managed `z`/`zi` functions.
- Removed unsupported Starship config key `trim_repo_root_to_cwd` and guarded `stty -ixon` behind a real TTY check.
- Created reusable VPS shell/bootstrap bundle at `~/workspace/vps-config` with guarded `.bashrc`, copied helper scripts, `install.sh`, `verify.sh`, `bootstrap-apt.sh`, `first-boot.sh`, `vps-doctor`, README, and local override example.
- Installed optional VPS dependencies on Ubuntu box: `docker.io`, `trash-cli`, `lm-sensors`, `multitail`, `shellcheck`, and Flutter via snap; enabled Docker and added `currentsuspect` to docker group.
- Published `currentsuspect/vps-config` GitHub repo and opened PR #1 adding shell-check CI; requested CodeRabbit review there.
- Merged VPS bundle work into existing public repo `currentsuspect/dotfiles` instead of creating a second long-term config source of truth.
- Audited `currentsuspect/dotfiles` and found tracked junk/sensitive-adjacent files in public history, especially `nvim/.config/nvim/undo/**` plus tracked Spicetify extracted/backup artifacts.
- Rewrote `currentsuspect/dotfiles` history with `git-filter-repo` to remove `nvim/.config/nvim/undo/**`, `spicetify/.config/spicetify/Extracted/**`, and `spicetify/.config/spicetify/Backup/**`; kept backup mirror at `~/workspace/dotfiles-history-scrub/dotfiles.git.backup`.
- Opened `currentsuspect/dotfiles` PR #1 (`cleanup-and-add-vps-shell`) to add `shell/` and `vps/` structure, root README/.gitignore, `.gitmodules`, and CI; after history scrub the PR dropped from too-large/noisy to reviewable size.
- Fixed major PR review findings in `dotfiles`: safer `safe-rm`, verify/doctor now fail on required issues, required vs optional command classification corrected, sudo-in-pipeline issues fixed, template/example files included in syntax/lint checks, `curl-time` made self-contained, `fw/json` hardened, Aestra helpers improved, storage cleanup/watch made safer, and `autoclean-remind` made opt-in.
- Current state at compaction: `dotfiles` PR #1 has green shell-check CI locally and on GitHub, CodeRabbit re-review has been triggered again after fixes, and the repo is moving toward a unified dotfiles layout with shared shell + VPS bootstrap while a future follow-up PR may normalize nesting (`home/`, `config/`, `vps/`, etc.).
